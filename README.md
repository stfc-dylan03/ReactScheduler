This repo contains a mock up of the scheduler made in Next.js (a React framework) using Typescript.

To run, navigate to `/react-scehduler-test` and run `npm run dev`

Currently each commit highlights a new feature added and shows how to do something. This is subject to change in the future.
